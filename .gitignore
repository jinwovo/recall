# ---- secrets / env ----
.env
.env.local
*.local

# ---- Java / Gradle ----
backend/.gradle/
backend/build/
backend/bin/
backend/out/
.gradle/
build/
*.class

# ---- Node / Next.js ----
frontend/node_modules/
frontend/.next/
frontend/out/
frontend/.turbo/
node_modules/
npm-debug.log*
yarn-error.log*
pnpm-debug.log*

# ---- Python (embedding sidecar / eval) ----
__pycache__/
*.py[cod]
.venv/
venv/
embedding-service/.venv/
eval/.venv/
.pytest_cache/
.ruff_cache/

# ---- model weights / data ----
models/
*.safetensors
*.bin
*.gguf
data/raw/
data/index/

# ---- IDE / OS ----
.idea/
.vscode/
*.iml
.DS_Store
Thumbs.db

# ---- Claude Code local settings ----
.claude/settings.local.json

# ---- docker / runtime volumes ----
.volumes/
*.log

# ---- demo recording scratch (GIF source videos) ----
frontend/demo-video/

# BEIR benchmark downloads and conversions — regenerate with `make beir` (eval/beir.py).
# The SciFact corpus alone is 8 MB, and the cache holds the raw pages it was built from.
eval/.beir-cache/
eval/beir-*/

# Host-run backend log (docker-compose.hostdev.yml flow).
backend-run.log

# run_eval.py --records cache: a resume aid, not a result (docs/beir-results.json is).
docs/beir-scores.json
